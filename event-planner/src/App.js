// Import the EventPlanner component to be rendered within the App
import EventPlanner from './components/EventPlanner';

// Main App component renders the EventPlanner inside a container div
function App() {
  return (
    <div className="App">
      <EventPlanner />
    </div>
  );
}

export default App;
